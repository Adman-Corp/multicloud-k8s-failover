gatewayClass:
  name: admancorp-envoy

gateway:
  name: platform-gateway
  namespace: platform-ingress
  className: admancorp-envoy
  hostname: "*.example.com"
  clusterIssuer: letsencrypt-production
